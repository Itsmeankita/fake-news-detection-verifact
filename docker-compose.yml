version: "3.8"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./model:/app/model   # persist trained model artifacts
      - ./data:/app/data
    environment:
      - SECRET_KEY=change-this-in-production
    restart: unless-stopped
